{
  "description": "Fellowship gate state machine — enforces phase gates for quest teammates. PreToolUse hooks intercept work tools to block activity at unapproved gates (gate-guard), detect gate submissions on SendMessage (gate-submit), and prevent premature task completion (completion-guard). PostToolUse hooks track /lembas invocations as phase prerequisites (gate-prereq) and record phase metadata on TaskUpdate (metadata-track).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|Bash|Agent|Skill|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/gate-guard.sh",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "SendMessage",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/gate-submit.sh",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "TaskUpdate",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/completion-guard.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/gate-prereq.sh",
            "timeout": 5
          }
        ]
      },
      {
        "matcher": "TaskUpdate",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/metadata-track.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
